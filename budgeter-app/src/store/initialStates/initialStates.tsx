import { initialFormState } from "store/reducers/formReducer";
import { initialState } from "store/reducers/budgetListReducer";

export const initialStates = {
    form: initialFormState,
    budgetList: initialState,
    amount:0,
}